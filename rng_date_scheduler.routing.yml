entity.rng_event_type.date_scheduler:
  path: '/admin/structure/rng/event_types/manage/{rng_event_type}/date_scheduler'
  defaults:
    _entity_form: 'rng_event_type.date_scheduler'
    _title: 'Date scheduler'
  requirements:
    _permission: 'administer event types'
